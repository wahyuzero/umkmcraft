import { TenantNotFound } from "@/components/TenantNotFound";

/**
 * 404 host tenant — dirancang self-sufficient: palet builder paper/ink/
 * cutline dengan aksen die-cut, bukan warna tema tenant.
 */
export default function SitesNotFound() {
  return <TenantNotFound />;
}
